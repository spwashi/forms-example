import {createContext} from "react";

/**
 * Provides the Context for a Form Item:
 *
 *  a Form Item is a named element of a form
 */
export const FormItemContext = createContext({name: undefined});
